/*
Ejercicio 2
Seleccione la respuesta correcta.




1. Las condiciones mínimas y necesarias para realizar una búsqueda secuencial sobre un vector son:
a. El vector debe estar ordenado.
b. El vector debe poseer un campo de tipo int.
c. El vector debe ser PUP.
d. Ninguna de las anteriores.

2. Para realizar un apareo ordenado de vectores se debe cumplir que:
a. Los vectores deben estar ordenados por el mismo campo y completos.
b. Los vectores deben estar ordenados de forma ascendente y completos.
c. Los vectores deben estar ordenados por el mismo campo.
d. Todas las anteriores.

3. El algoritmo de búsqueda binaria, respecto de la búsqueda secuencial:
a. Incrementa la cantidad de comparaciones
b. Reduce la cantidad de comparaciones
c. Realiza la misma cantidad de comparaciones
d. No realiza comparaciones como la búsqueda secuencial

4. #define es una directiva para:
a. el compilador
b. el preprocesador
c. el debugger


*/

#include <iostream>
using namespace std;

int main()
{

    return 0;
}